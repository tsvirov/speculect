## What does this PR do?

## Checklist

- [ ] `pytest -q` passes locally
- [ ] `ruff check .` passes locally
- [ ] No `X | Y` union syntax or `match` statements added (Python 3.9 compat)
- [ ] README updated if a CLI flag or behavior changed
- [ ] Tests added/updated for the change
